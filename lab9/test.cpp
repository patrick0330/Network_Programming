#include <bits/stdc++.h>

using namespace std;

int main(){
    char buf[256];
    // scanf("%s", buf);
    // for(int i = 0;i < sizeof(buf);i++){
    //     printf("%c", buf[i]);
    // }
    int i = 1;
    sprintf(buf,"Hi %d", i);
    printf("%s\n", buf);
    

}